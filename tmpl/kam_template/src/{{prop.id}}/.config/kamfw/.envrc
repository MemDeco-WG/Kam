# Runtime configuration loaded by lib/kamfw/.kamfwrc.
# Keep module-local state under the installed module directory.
KAMFW_DIR="${KAMFW_DIR:-${MODDIR}/lib/kamfw}"
KAM_HOME="${KAM_HOME:-${MODDIR}}"
KAM_MODULES="${KAM_MODULES:-}"
export KAMFW_DIR KAM_HOME KAM_MODULES
